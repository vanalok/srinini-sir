.git
.gitignore
.assetsignore
.wrangler
wrangler.toml
node_modules
# 895-page survey PDF exceeds Cloudflare's 25MB asset limit; served on GoDaddy instead
assets/pdf/f453f3_8f29666e401c413ebf0b7e95d805db5e.pdf

# Oversized original CV (25MB, double .pdf.pdf name) — compressed copy served as assets/Srinivasulu-IFS-CV.pdf
assets/Srinivasulu-IFS-CV.pdf.pdf

# PHP admin CMS — never serve on Cloudflare (static host cannot run PHP)
admin

# PHP backend/API — contains DB credentials (api/db.php); never serve on Cloudflare (static host cannot run PHP)
api

# Local secrets — never serve on Cloudflare
.wix-credentials.local
*.local

# Stray file from an unrelated project (Vanalok) — not part of this site
images/index.php
